package tests

import org.scalatest.FunSuite

/**
 * @author Michael
 */
class LibraryTests extends FunSuite {
  test("load folder") {
    //    val (json, duration) = Utils.timed(Json.toJson(Library.folderFromFile(testFolder)))
    //    val (json, duration) = Utils.timed(Json.toJson(Library.rootFolderFromDatabase))
    //    println(json)
    //    println(s"Serialized $testFolder in $duration")
  }
  test("list files") {
    //    val (folderCount, folderDuration) = Utils.timed(Library.folderStream.size)
    //    println(s"Got $folderCount folders in $folderDuration")
    //    val (fileCount, fileDuration) = Utils.timed(Library.tracksStream.size)
    //    println(s"Got $fileCount files in $fileDuration")
  }
}
